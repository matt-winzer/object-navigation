const ability = require('./pokemon.json')

// Write a function that takes in an ability object, and returns (or logs):
  // The ability name



// Write a function that takes in an ability object, and returns (or logs):
  // An object with the ability name and generation



// Write a function that takes in an ability object, and returns (or logs):
  // An array of all the pokemon that can use this ability



// Write a function that takes in an ability object, and returns (or logs):
  // An object with the ability id, name, and an array of all the pokemon that can use the ability

